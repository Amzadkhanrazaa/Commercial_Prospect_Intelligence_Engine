CREATE TABLE prospects (
    lead_id INT PRIMARY KEY,
    company_size INT,
    annual_revenue BIGINT,
    industry_score INT,
    previous_interactions INT,
    credit_score INT,
    converted INT
);